# 2026-04-20 工作记录

## Hailu-SC-EMS 项目初始化与开发

### 项目基本信息
- 项目路径：`e:/Kingsley/workbuddy/数据管理系统/Hailu-SC-EMS/frontend/`
- 技术栈：React 19 + TypeScript 5.9 + Vite 7 + Tailwind CSS v4 + Supabase
- 后端：Supabase 云端（URL: mdukduvdzwxfheyqvkfy.supabase.co）

### 完成的工作

#### 跨平台修复（macOS → Windows）
1. `test_schema.cjs` 第12行 `/tmp/schema_output.json` → `./schema_output.json`
2. `index.html` title 从 `frontend` 改为 `HAILU SC-EMS`
3. `App.css` 清理 Vite 模板残留样式

#### 新增文件
- `src/types/` 目录：organization.ts, client.ts, invoice.ts, contract.ts, expense.ts, user.ts, index.ts
- `src/hooks/useInvoices.ts`：从 InvoiceManager.tsx 抽取的数据获取 hook
- `src/features/financial/ContractManager.tsx`：合同管理完整组件
- `src/features/financial/ExpenseManager.tsx`：贸易费用完整组件（支持多币种）
- `supabase_migration.sql`：Supabase contracts + expenses 建表 SQL（含 RLS）

#### 修改文件
- `src/features/financial/FinancialManager.tsx`：接入 ContractManager 和 ExpenseManager，替换占位符
- `src/features/dashboard/GroupDashboard.tsx`：修复 TS 类型错误（JSON 断言、移除未使用 import）
- `src/App.tsx`、`src/features/auth/UserManager.tsx`：移除未使用的 React import

#### 构建状态
- `npm run build` 通过，ExitCode: 0，691 个模块，产出 dist/

#### 热力图筛选功能（新增）
- 文件：`src/features/dashboard/GroupDashboard.tsx`
- 改动：`rawRecords` 保留所有发票原始数据，`filteredRecords` 由筛选状态驱动派生计算
- 三组筛选：市场下拉、商品下拉（product_info 字段）、日期范围（trade_date 字段）
- 地图 Tooltip 增强：hover 气泡展示该市场的商品明细分类（按营收排序，最多显示5个）
- 顶部统计卡片、子公司列表均随筛选联动
- `npm run build` 通过

### 待完成
- Supabase 需手动在 Dashboard 执行 `supabase_migration.sql` 创建 contracts 和 expenses 表
- 可进一步在 vite.config.ts 配置代码分割（chunk > 500KB 警告）
